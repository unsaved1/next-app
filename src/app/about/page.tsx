import {Metadata} from "next";

export const metadata: Metadata = {
    title: 'about',
    description: 'about page'
}

const Page = () => {
    return (
        <h1>about page</h1>
    )
};

export default Page;